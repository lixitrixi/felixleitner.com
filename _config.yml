domain: felixleitner.com

plugins:
  - jekyll-pug

collections_dir: collections

collections:
  skills:
    output: false
  projects:
    output: true
    permalink: /project/:slug

defaults:
  -
    scope:
      path: ""
      type: "pages"
    values:
      layout: "default"
  -
    scope:
      path: "project"
      type: "projects"
    values:
      layout: "project"
